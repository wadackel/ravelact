name: 'Setup Build'
description: 'Composite that prepares the build environment'
inputs:
  toolchain:
    description: 'Toolchain version'
    required: false
    default: 'stable'
runs:
  using: 'composite'
  steps:
    - run: echo prepare ${{ inputs.toolchain }}
      shell: bash
