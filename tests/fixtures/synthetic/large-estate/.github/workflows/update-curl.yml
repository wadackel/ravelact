name: Update curl
on:
  schedule:
    - cron: "0 4 * * 0"
  workflow_dispatch:
permissions:
  contents: write
  pull-requests: write
jobs:
  check-update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo check curl
      - uses: peter-evans/create-pull-request@v7
        with:
          branch: deps/update-curl
          title: "deps: update curl"
          body: "Auto-bump for curl"
