name: Update sqlite
on:
  schedule:
    - cron: "0 4 * * 0"
  workflow_dispatch:
permissions:
  contents: write
  pull-requests: write
jobs:
  check-update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo check sqlite
      - uses: peter-evans/create-pull-request@v7
        with:
          branch: deps/update-sqlite
          title: "deps: update sqlite"
          body: "Auto-bump for sqlite"
