name: Release
on:
  push:
    tags: ["v*"]
permissions:
  contents: write
  id-token: write
jobs:
  call-publish:
    uses: ./.github/workflows/_reusable-publish.yml
    secrets: inherit
    with:
      tag: ${{ github.ref_name }}
