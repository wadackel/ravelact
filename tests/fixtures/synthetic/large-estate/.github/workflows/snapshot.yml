name: Snapshot
on:
  schedule:
    - cron: "0 1 * * *"
jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/publish-artifact
        with:
          name: snapshot
