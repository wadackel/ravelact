name: Coverage
on:
  push:
    branches: [main]
jobs:
  coverage:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        node-version: ["20", "22"]
    steps:
      - uses: actions/checkout@v4
      - uses: codecov/codecov-action@v4
      - run: echo coverage
